# Python
__pycache__/
*.pyc
.venv/
venv/
backend/.env

# Node
node_modules/
frontend/dist/
frontend/.vite/

# Data pack (kept out of the public repo). The deployed app receives it at runtime via a
# base64 secret (see DATA_PACK_B64 in app/core/knowledge.py).
data/*.pdf
data/*.xlsx

# OS
.DS_Store
Thumbs.db
